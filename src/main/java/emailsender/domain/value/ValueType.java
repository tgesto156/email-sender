package emailsender.domain.value;

import lombok.Getter;

@Getter
public enum ValueType {

    TOTAL_VALUE,
    CLIENT_NAME,
    REDIRECT_URL,
    REFERENCE_NUMBER,
    REFERENCE_DATE,
    IDENTIFIER,
    TRANSACTION_ID,
    AMOUNT,
    DATE,
    REFERENCE,
    ENTITY,
    RESETPASSWORDURL,
}